<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - NotesApp</title>
    <link rel="stylesheet" href="css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="img/logoo.png" type="image/x-icon">
</head>
<body class="auth-page">
    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-header">
                <div class="brand">
                    <img src="img/logoo.png" alt="Logo del Sistema" height="200">
                </div>
                <h1 class="auth-title">Bienvenido de vuelta</h1>
                <p class="auth-subtitle">Inicia sesión en tu cuenta para continuar</p>
            </div>

            <% if (typeof mensaje != 'undefined') { %>
                <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
                <script>
                    Swal.fire({
                        icon: 'info',
                        title: 'Información',
                        text: '<%= mensaje %>',
                        confirmButtonText: 'Aceptar'
                    });
                </script>
            <% } %>
            <form class="auth-form" id="loginForm" action="/LoginA" method="post">
                <div class="form-group">
                    <label for="cedula" class="form-label">Cedula de Identidad</label>
                    <div class="input-group">
                        <i class="fas fa-id-card input-icon"></i>
                        <input type="text" id="cedula" name="cedula" class="form-input" placeholder="Ej: 12345678">
                    </div>
                </div>

                <div class="form-group">
                    <label for="password" class="form-label">Contraseña</label>
                    <div class="input-group">
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" id="password" name="password" class="form-input" placeholder="Tu contraseña">
                        <button type="button" class="password-toggle" id="togglePassword">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>

                <div class="form-options">
                    <a href="#" class="forgot-link">¿Olvidaste tu contraseña?</a>
                </div>

                <button type="submit" class="btn btn-primary btn-full btn-large">
                    <i class="fas fa-sign-in-alt"></i>Iniciar Sesión
                </button>
            </form>

            <div class="auth-footer">
                <p>¿No tienes una cuenta? <a href="/registro" class="auth-link">Regístrate aquí</a></p>
                <p>¿Olvidaste tu contraseña? <a href="/recuperar" class="auth-link">Recuperar contraseña</a></p>
                <p>¿Quieres volver al inicio? <a href="/" class="auth-link">Ir al inicio</a></p>
            </div>
        </div>

        <div class="auth-visual">
            <div class="visual-content">
                <h2>Sistema de Gestion de Notas</h2>
                <p>Accede a todas tus notas desde cualquier dispositivo y mantén todo sincronizado.</p>
                <div class="visual-features">
                    <div class="visual-feature">
                        <i class="fas fa-sync-alt"></i>
                        <span>Sincronización automática</span>
                    </div>
                    <div class="visual-feature">
                        <i class="fas fa-shield-alt"></i>
                        <span>Seguridad avanzada</span>
                    </div>
                    <div class="visual-feature">
                        <i class="fas fa-mobile-alt"></i>
                        <span>Acceso multiplataforma</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="js/login.js"></script>
</body>
</html>
